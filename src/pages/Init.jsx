const Init = () => {
    return (
        <div>
            Página inicial
            <button>Iniciar Sesión</button>
            <button>Regístrate!</button>
        </div>
    )
}

export default Init
